const User = require('../model/User');

// const addContact = async (socket, data) => {
// 	// console.log(data);
// 	// socket.emit('test', 'ok');

// };

const sendMessage = async (sender, receiver, msg) => {};

module.exports = {
	sendMessage,
};
