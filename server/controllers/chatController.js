const User = require('../model/User');

const addMessage = async (req, res) => {
	console.log('adding message');
	if (!req?.body?.contact || !req?.body?.msg) {
	}
};

const getMessages = async (req, res) => {};

module.exports = {};
